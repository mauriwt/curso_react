import React, { useState } from 'react';

const LoginForm = () => {

    const inicialData = [{
        user: '',
        password: ''
    }]

    const [data, setData] = useState(inicialData);

    return (
        <div>
            
        </div>
    );
}

export default LoginForm;
